name: Build alcalculator

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up MinGW
        run: choco install mingw

      - name: Compile C program to .exe
        run: echo pwd && echo dir
