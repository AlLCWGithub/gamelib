name: Build tictactoe
on:
  workflow_dispatch
jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup compiler (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          choco install mingw
          gcc --version
      - name: Setup compiler (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get remove gcc -y
          sudo apt-get update
          sudo apt-get install gcc-aarch64-linux-gnu
          aarch64-linux-gnu-gcc --version
      - name: Compile .c to .exe (Windows)
        if: matrix.os == 'windows-latest'
        run: gcc tictactoe.c -o tictactoe.exe
      - name: Compile .c to .out (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: gcc tictactoe.c -o tictactoe.out
      - name: Upload artifact (Windows)
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: tictactoe-windows
          path: tictactoe.exe
      - name: Upload artifact (Linux)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: tictactoe-linux
          path: tictactoe.out
